// Tipos globales de Next.js (módulos CSS, imágenes, `server-only`).
// `next-env.d.ts` lo genera `next build`/`next dev` y está en .gitignore; este
// archivo permite que `npm run typecheck` funcione también en un clon limpio.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
